# Testing Data

This directory holds held-out VODs used exclusively for evaluation.
Data placed here must never be used during model development, calibration,
or parameter tuning. It exists solely to measure generalization performance.

Currently empty — add streams here after the initial training pipeline is built.
